export const metadata = {
  title: "Partner with Us | Registration",
  description: "Register as a Manufacturer or Wholesaler with NariZari and scale your business.",
};

export default function JoinLayout({ children }) {
  return <>{children}</>;
}
