export const metadata = {
  title: "Contact Us",
  description: "Get in touch with NariZari to scale your heritage brand and discuss your marketing needs.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
